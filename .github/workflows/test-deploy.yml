name: Django TODO project test for api blogs and accounting

on:
  push:
    branches: [ "Excersise11" ]
  pull_request:
    branches: [ "Exersise11" ]

jobs:

  Tests:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: setup docker compose
      run: docker-compose up -d
    - name: run test for pytest config
      run: docker exec backend sh -c "flake8 && pytest ."

  Deploy:
    if: ${{ always() && contains(join(needs.*.result, ','), 'success') }}
    name: Deploy 
    needs: Tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: ssh to vps ubuntu
      uses: appleboy/ssh-action@master
      with:
        username: ${{secrets.USERNAME}}
        password: ${{secrets.PASSWORD}}
        host: ${{secrets.HOST_VPS}}
        port: ${{secrets.PORT}}
        script: |
          docker-compose -f ./TODO/docker-compose-stage.yml stop
          git pull
          docker-compose -f ./TODO/docker-compose-stage.yml start
